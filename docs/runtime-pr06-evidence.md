# PR 00–06 验收记录

> 历史设计 / 阶段验收记录：文中的分支、角色与未实现项以记录当时为准。当前 v1.3.0 使用 [标准模式](runtime-standard-mode.md) 和 [主机超管控制台](superadmin-console.md)；早期验证结果不代表当前完整生产验收。

2026-09-17 · 分支 `codex/runtime-standard-pr06` · 本机 macOS

## 交付结果

可信团队标准模式可在 Web 管理端内部使用：超管连接 Codex、按模型授权 admin、新会话固定引擎与连接、持续运行与审批、文档和产物，以及受 actor 作用域限制的业务工具。

这是单 API 调度进程的本地实现。DeepAgents 通过共同聊天适配入口保持原 LangGraph 执行路径；新增持久 CLI 队列用于 Codex。Docker、Cloudflare、Cursor、多副本与公开消费者入口没有启用。

## 提交边界

| 编号 | 提交 | 内容 |
|---|---|---|
| 00 | `2be6d9a` | 收口已有单管理员 Codex 试验，保留原工作区未提交改动 |
| 01 | `831bf1a` | 共享供应商身份、独立会话与恢复验证 |
| 02 | `7173852` | 显式 owner 身份、部署策略和不可用后端拒绝 |
| 03 | `c99f67b` | 有界持久队列、本机执行与事件重放 |
| 04 | `d6c772b` | 设置页连接管理、加密凭据和登录生命周期 |
| 05 | `4cc0bbf` | 可信 admin 模型授权、代次和撤销 |
| 06 | 本文所在提交 | 主聊天、业务工具、实际验收与操作文档 |

尚未创建远端 GitHub PR，也未推送到公开仓库。

## 自动验证

后续自审补充为 61 项 Runtime 测试，并修复异常分支；详见 [自审记录](runtime-pr06-self-review.md)。以下保留初次验收计数。

- Runtime：46 项通过，包含权限、账号代次、幂等、排队、审批、撤销、重启、凭据、文件边界、聊天适配及独立进程组清理。
- 本地存储：22 项通过。
- 前端 TypeScript / Vite production build：通过。现有大 bundle 警告仍存在。
- DeepAgents：旧 SSE 文本 / 工具事件和历史写入走实际代码路径回归，使用模拟模型事件；没有进行真实上游 DeepAgents 推理验收。

## 真实 Codex 验证

CLI `0.154.0-alpha.6.2`，模型 `gpt-5.6-sol`，使用已有专用个人登录缓存；未改用 API Key。

| 验证 | 结果 |
|---|---|
| 同一供应商身份供两个合成 admin 使用 | 通过；独立 HOME、工作区、thread，顺序租用刷新缓存 |
| 多轮续话和状态恢复 | 通过；恢复原 admin 标记，不混入另一 admin 的标记 |
| 文档 / 个人记忆 / 当前 admin 服务文档 | 真实工具调用通过，记忆更新写回后核对 |
| actor 自定义系统提示 | 真实输出确认生效 |
| 主聊天 owner 与 admin | 浏览器完成真实文本；admin 完成审批和文件预览 |
| 浏览器离开、刷新和重开 | 运行继续；恢复输出 / 待审批；未重复提交任务 |
| 交叉会话、审批、产物读取 | 拒绝；HTTP 404 / 权限测试覆盖 |
| 撤销待审批授权 | 原 Codex 进程退出，临时 auth.json 删除；重新授权不能续用旧绑定 |
| 已批准的长命令取消 | 真实子进程退出；独立进程组遗漏在验收中发现并修复，增加回归测试 |
| 设备码登录界面 | 真实官方挑战已展示并成功取消；本次没有完成新的浏览器账号授权 |
| 登录取消、过期、重复、旧回调、账号切换 | 自动协议测试通过；第二个真实供应商身份未测 |

测试账号仅用于 loopback 验收，不作为开发版持久登录账号。登录缓存、个人邮箱、账号 token 和真实事件库不进入 Git。

## 延迟记录

这些是少量本机真实样本，不代表性能承诺，也没有相同 prompt 的旧实现对照。

| 场景 | 首段文本（含排队） | 总时间 |
|---|---:|---:|
| owner 文档、记忆、审批和产物 | 14.58 秒 | 33.04 秒 |
| admin A 文本 | 14.41 秒 | 14.59 秒 |
| admin B 文本 | 14.39 秒 | 14.91 秒 |
| admin A 续话 | 8.29 秒 | 8.30 秒 |

浏览器另一次主聊天样本：首段 11.18 秒，26 个持久文本批次，批次间隔中位数 0.109 秒、最大 0.505 秒。文本写盘按 60ms / 1024 字符合并，审批与终态仍事务持久化。供应商开始输出前的等待没有宣称已消除。

## 后续验收边界

- 原生生图未验证真实图片输出，保持关闭；没有付费图片 API 回退。
- Cloudflare Containers / DO / Workers 和两个独立供应商身份未验证；不能据此宣称隔离模式可用。
- Cursor、Docker、Windows、多副本、消费者 Web、微信、定时任务和语音迁移属于后续范围。
- 标准模式只面向可信 admin；本机 HOME 分开和应用权限不等于恶意租户 OS 隔离。
- 当前连接并发 1、全局 / admin 队列有上限；商业 ToC 发布仍需 PR 07 之后的工作。

详见 [运行、故障恢复与回退说明](runtime-standard-mode.md)。
